import { TypedSchema, object, typed } from '@speakeasy-api/tonic';
/**
 * Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
 */
export interface PostEntitlementsFeaturesRequestMetadata {
}

export const PostEntitlementsFeaturesRequestMetadataSchema: TypedSchema<PostEntitlementsFeaturesRequestMetadata> = typed<PostEntitlementsFeaturesRequestMetadata>(object({
}));