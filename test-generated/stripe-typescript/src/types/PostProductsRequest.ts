import { TypedSchema, array, boolean, object, optional, string, typed } from '@speakeasy-api/tonic';

import type { PostAccountsAccountBankAccountsRequestMetadata } from './PostAccountsAccountBankAccountsRequestMetadata';
import { PostAccountsAccountBankAccountsRequestMetadataSchema } from './PostAccountsAccountBankAccountsRequestMetadata';
import type { PostProductsRequestDefaultPriceData } from './PostProductsRequestDefaultPriceData';
import { PostProductsRequestDefaultPriceDataSchema } from './PostProductsRequestDefaultPriceData';
import type { PostProductsRequestMarketingFeaturesItem } from './PostProductsRequestMarketingFeaturesItem';
import { PostProductsRequestMarketingFeaturesItemSchema } from './PostProductsRequestMarketingFeaturesItem';
import type { PostProductsRequestPackageDimensions } from './PostProductsRequestPackageDimensions';
import { PostProductsRequestPackageDimensionsSchema } from './PostProductsRequestPackageDimensions';
export interface PostProductsRequest {
  /**
   * Whether the product is currently available for purchase. Defaults to `true`.
   */
  active?: boolean;
  /**
   * price_data_without_product_with_metadata
   *
   * Data used to generate a new [Price](https://stripe.com/docs/api/prices) object. This Price will be set as the default price for this product.
   */
  defaultPriceData?: PostProductsRequestDefaultPriceData;
  /**
   * The product's description, meant to be displayable to the customer. Use this field to optionally store a long form explanation of the product being sold for your own rendering purposes.
   */
  description?: string;
  /**
   * Specifies which fields in the response should be expanded.
   */
  expand?: Array<string>;
  /**
   * An identifier will be randomly generated by Stripe. You can optionally override this ID, but the ID must be unique across all products in your Stripe account.
   */
  id?: string;
  /**
   * A list of up to 8 URLs of images for this product, meant to be displayable to the customer.
   */
  images?: Array<string>;
  /**
   * A list of up to 15 marketing features for this product. These are displayed in [pricing tables](https://stripe.com/docs/payments/checkout/pricing-table).
   */
  marketingFeatures?: Array<PostProductsRequestMarketingFeaturesItem>;
  /**
   * Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
   */
  metadata?: PostAccountsAccountBankAccountsRequestMetadata;
  /**
   * The product's name, meant to be displayable to the customer.
   */
  name: string;
  /**
   * package_dimensions_specs
   *
   * The dimensions of this product for shipping purposes.
   */
  packageDimensions?: PostProductsRequestPackageDimensions;
  /**
   * Whether this product is shipped (i.e., physical goods).
   */
  shippable?: boolean;
  /**
   * An arbitrary string to be displayed on your customer's credit card or bank statement. While most banks display this information consistently, some may display it incorrectly or not at all.

This may be up to 22 characters. The statement description may not include `<`, `>`, `\`, `"`, `'` characters, and will appear on your customer's statement in capital letters. Non-ASCII characters are automatically stripped.
 It must contain at least one letter. Only used for subscription payments.
   */
  statementDescriptor?: string;
  /**
   * A [tax code](https://stripe.com/docs/tax/tax-categories) ID.
   */
  taxCode?: string;
  /**
   * A label that represents units of this product. When set, this will be included in customers' receipts, invoices, Checkout, and the customer portal.
   */
  unitLabel?: string;
  /**
   * A URL of a publicly-accessible webpage for this product.
   */
  url?: string;
}

export const PostProductsRequestSchema: TypedSchema<PostProductsRequest> = typed<PostProductsRequest>(object({
  active: optional(boolean()),
  get defaultPriceData() { return optional(PostProductsRequestDefaultPriceDataSchema) },
  description: optional(string()),
  expand: optional(array(string())),
  id: optional(string()),
  images: optional(array(string())),
  get marketingFeatures() { return optional(array(PostProductsRequestMarketingFeaturesItemSchema)) },
  get metadata() { return optional(PostAccountsAccountBankAccountsRequestMetadataSchema) },
  name: string(),
  get packageDimensions() { return optional(PostProductsRequestPackageDimensionsSchema) },
  shippable: optional(boolean()),
  statementDescriptor: optional(string()),
  taxCode: optional(string()),
  unitLabel: optional(string()),
  url: optional(string()),
}));