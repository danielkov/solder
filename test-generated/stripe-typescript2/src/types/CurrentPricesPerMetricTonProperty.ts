import { object, typed } from '@speakeasy-api/tonic';
import type { TypedSchema } from '@speakeasy-api/tonic';
/**
 * Current prices for a metric ton of carbon removal in a currency's smallest unit.
 */
export interface CurrentPricesPerMetricTonProperty {}

export const CurrentPricesPerMetricTonPropertySchema: TypedSchema<CurrentPricesPerMetricTonProperty> = typed<CurrentPricesPerMetricTonProperty>(object({}));
